import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DesignPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Design</CardTitle>
        <CardDescription>Details about the AI system's design.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the design page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
