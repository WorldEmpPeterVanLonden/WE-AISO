import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function LifecyclePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Lifecycle Overview</CardTitle>
        <CardDescription>Manage the project's lifecycle model and reviews.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the lifecycle page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
