import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function TrainingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Training</CardTitle>
        <CardDescription>Details about the model training phase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the training page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
