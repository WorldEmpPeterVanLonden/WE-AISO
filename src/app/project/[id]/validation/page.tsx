import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function ValidationPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Validation</CardTitle>
        <CardDescription>Details about the validation phase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the validation page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
