import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function OperationPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Operation</CardTitle>
        <CardDescription>Details about the operation phase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the operation page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
