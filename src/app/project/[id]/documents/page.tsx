import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DocumentsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Documents</CardTitle>
        <CardDescription>Manage and export project documents.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the documents page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
