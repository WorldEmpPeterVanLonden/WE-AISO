import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function GovernancePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Governance</CardTitle>
        <CardDescription>Details about the project's governance and controls.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the governance page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
