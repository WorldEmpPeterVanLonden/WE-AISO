import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function RetirementPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Retirement</CardTitle>
        <CardDescription>Details about the retirement phase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the retirement page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
