import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function DevelopmentPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Development</CardTitle>
        <CardDescription>Details about the development phase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is the development page. Content to be added.</p>
      </CardContent>
    </Card>
  );
}
