
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow users to read and write if they are authenticated.
    // This is a permissive rule for development and should be
    // secured before going to production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
